/**
 * Next.js Configuration Template
 * ==============================
 * 
 * Purpose: Standard Next.js configuration with common patterns and options
 * Usage: Copy to project root, rename to next.config.js, and substitute variables
 * Last Updated: 2025-01-26
 *
 * Required Variables:
 * - {{IMAGE_DOMAINS}}: Allowed image domains (e.g., "'localhost', 'example.com'")
 * - {{REACT_STRICT_MODE}}: Enable React strict mode (true/false)
 *
 * Example substitution:
 * sed -i "s/{{IMAGE_DOMAINS}}/'localhost', 'cdn.example.com'/g" next.config.js
 *
 * @type {import('next').NextConfig}
 */
const nextConfig = {
  images: {
    domains: [{{IMAGE_DOMAINS}}],
  },
  // Environment-specific configurations
  reactStrictMode: {{REACT_STRICT_MODE}},
  
  // Optional: Custom webpack configuration
  webpack: (config, { buildId, dev, isServer, defaultLoaders, webpack }) => {
    // Custom webpack modifications can be added here
    // Example: Add custom loaders or plugins
    
    return config;
  },
  
  // Optional: Custom environment variables
  env: {
    // Add custom environment variables that should be available at build time
    // CUSTOM_ENV_VAR: process.env.CUSTOM_ENV_VAR,
  },
  
  // Optional: Redirect and rewrite rules
  async redirects() {
    return [
      // Add custom redirects here
      // {
      //   source: '/old-route',
      //   destination: '/new-route',
      //   permanent: true,
      // },
    ];
  },
  
  async rewrites() {
    return [
      // Add custom rewrites here
      // {
      //   source: '/api/custom/:path*',
      //   destination: '/api/handlers/:path*',
      // },
    ];
  },
}

module.exports = nextConfig